{% extends 'base.html.twig' %}

{% block title %}Création compte{% endblock %}

{% block body %}
    {% for flash_error in app.flashes('verify_email_error') %}
        <div class="alert alert-danger" role="alert">{{ flash_error }}</div>
    {% endfor %}

    

    {{ form_start(registrationForm, {'attr': {'class': 'form-full'}} ) }}
        <h1 class="titre">Création d'un compte client</h1>
        {{ form_label(registrationForm.nom) }}{{ form_widget(registrationForm.nom) }}
        {{ form_label(registrationForm.prenom) }}{{ form_widget(registrationForm.prenom) }}
        {{ form_row(registrationForm.email) }}
        {{ form_widget(registrationForm.plainPassword) }}
        {{ form_label(registrationForm.agreeTerms) }}{{ form_widget(registrationForm.agreeTerms) }}
        {{ form_label(registrationForm.newletter) }}{{ form_widget(registrationForm.newletter) }}
        <p></p>
        <button type="submit" class="btn-account">S'enregistrer</button>
    {{ form_end(registrationForm) }}
{% endblock %}
